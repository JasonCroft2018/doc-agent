# 🧪 Agent 项目测试技能手册 (TEST_SKILL.md)

> **定位**：从 0 到 1 构建 Agent 项目的测试体系方法论。
> 
> 以后任何 Agent 项目，按此手册可完成 单元测试→集成测试→验收测试→回归测试 的全闭环。
>
> **这不是流水账，这是测试策略、用例设计、踩坑记录、自动化框架。**
>
> 版本：v0.1（Phase 1-4 完成）
> 最后更新：2026-06-10
> 对应项目：DocAgent（企业文档智能助手）

---

## 目录

- [一、测试哲学](#一测试哲学)
- [二、测试金字塔（三层结构）](#二测试金字塔三层结构)
- [三、单元测试策略](#三单元测试策略)
- [四、集成测试策略](#四集成测试策略)
- [五、验收测试清单](#五验收测试清单)
- [六、自动化测试框架](#六自动化测试框架)
- [七、踩坑记录](#七踩坑记录)
- [八、面试话术](#八面试话术)

---

## 一、测试哲学

### 为什么 Agent 项目需要体系化测试？

Agent 项目不同于传统 Web 项目——它有 **LLM 推理的不确定性**：

```
传统 Web 项目：输入 A → 逻辑 B → 输出 C（确定性的）
Agent 项目：   输入 A → LLM 推理 → 输出 ？      （非确定性的）
```

因为 LLM 的输出不是固定的，所以测试的重点从"断言精确返回值"变成了**"断言行为模式"**。

### 测试的三个核心原则

| 原则 | 说明 | 实践 |
|:----|:----|:-----|
| **确定性优先** | 框架代码（切片器、检索器、API 模型）做精确断言 | `assert result == expected` |
| **非确定性用模式匹配** | LLM 输出测试用关键词/结构匹配 | `assert "你好" in result` |
| **分层验证** | 单元测逻辑、集成测流程、验收测体验 | 三层互不替代 |

---

## 二、测试金字塔（三层结构）

```
           🔍
         🧑‍💻 验收测试（手动）
        ┌───────────────────┐
        │  browser + 人工验证 │
        │  场景：完整交互流程  │
        └─────────┬─────────┘
                  │
         ⚙️  集成测试（脚本自动化）
        ┌───────────────────┐
        │  API 端点全链路验证  │
        │  场景：Health/Chat/  │
        │  Search/页面        │
        │  工具：bash + curl  │
        └─────────┬─────────┘
                  │
         🧩  单元测试（pytest 自动化）
        ┌───────────────────┐
        │  单个函数/类独立验证  │
        │  场景：切片器/检索器/  │
        │  API 模型/Agent 状态 │
        │  工具：pytest       │
        └───────────────────┘
```

| 层级 | 自动化 | 执行频率 | 责任人 |
|:----|:-----:|:--------|:------|
| 单元测试 | ✅ 全自动 | 每次代码修改 | 开发者 |
| 集成测试 | ✅ 全自动 | 每次启动前 | CI/CD |
| 验收测试 | ❌ 手动 | 发布前 | QA/PM |

---

## 三、单元测试策略

### 3.1 覆盖范围

| 测试文件 | 覆盖模块 | 用例数 | 确定性？ |
|:--------|:--------|:-----:|:-------:|
| `test_splitter.py` | 语义切片器 | 6 | ✅ 完全确定 |
| `test_retriever.py` | 混合检索器 | 2 | ✅ 完全确定 |
| `test_agent.py` | Agent 状态、意图识别兜底 | 5 | ✅ 完全确定 |
| `test_mcp.py` | MCP 客户端工具包装 | 4 | ✅ 完全确定 |
| `test_api.py` | API 数据模型 | 7 | ✅ 完全确定 |

**特点**：单元测试全部是**确定性测试**——不依赖 LLM，不调用外部服务，纯逻辑验证。

### 3.2 用例设计方法论

#### 语义切片器（6 个用例）

| 用例 | 测试场景 | 验证点 |
|:----|:--------|:-------|
| `test_parse_simple_heading` | 单标题文档 | 标题被正确提取 |
| `test_parse_multi_headings` | 多级标题 | 层级解析正确 |
| `test_parse_hierarchy` | 标题层级栈 | 子标题知道父标题是谁 |
| `test_chunk_small_text` | 短文本不分块 | ≤chunk_size 的不切 |
| `test_chunk_large_text` | 长文本分块 | >chunk_size 的切分 |
| `test_chunk_with_hierarchy` | 带层级的分块 | 每块保留标题前缀 |

**设计思路**：覆盖正常路径（简单/多级/层级）、边界条件（短文本/长文本）、功能验证（层级保留）。

#### API 模型（7 个用例）

| 用例 | 测试场景 |
|:----|:--------|
| `test_chat_request` | ChatRequest 字段赋值 |
| `test_chat_request_default_thread` | 默认 thread_id 验证 |
| `test_search_request` | SearchRequest 字段 |
| `test_search_request_defaults` | 默认 top_k=5 |
| `test_search_request_top_k_range` | top_k 边界校验（<1） |
| `test_health_response` | HealthResponse 构造 |
| `test_chat_response_with_sources` | ChatResponse 带来源 |

**设计思路**：覆盖正常赋值、默认值、边界值、带嵌套对象的构造。

### 3.3 运行命令

```bash
# 单个测试文件
python3 tests/test_splitter.py

# 全部测试
for f in tests/test_*.py; do echo "=== $f ==="; python3 "$f"; done
```

---

## 四、集成测试策略

### 4.1 测试范围

集成测试覆盖 **API 端点全链路**，验证服务能正常启动和响应：

```
测试流程：
API 进程 → 端口监听 → Health → Stats → Chat(问候) → Chat(检索) → Search → 页面
```

### 4.2 9 项集成测试

| 编号 | 测试项 | 命令 | 预期 | 耗时 |
|:---:|:------|:----|:----|:---:|
| T1 | 进程检查 | `ps aux \| grep uvicorn` | PID 存在 | <1s |
| T2 | 端口监听 | `lsof -i :8765` | LISTEN 状态 | <1s |
| T3 | GET /api/health | `curl .../api/health` | status=ok | <1s |
| T4 | GET /api/stats | `curl .../api/stats` | 返回统计 | <1s |
| T5 | POST /api/chat（问候） | `curl -X POST .../api/chat` | intent=chat | ~1-60s* |
| T6 | POST /api/chat（检索） | `curl -X POST .../api/chat` | intent=retrieve | ~1-60s* |
| T7 | POST /api/search | `curl -X POST .../api/search` | results=[] | ~1-5s |
| T8 | GET /（页面） | `curl .../` | 含 DocAgent | <1s |
| T9 | Ollama 模型检查 | `curl .../api/tags` | 三个模型 | <1s |

> *T5/T6 耗时取决于 LLM 模型：qwen2.5:3b ~1s，deepseek-r1:14b ~60s

### 4.3 集成测试脚本

**位置**：`tests/run_test.sh`

```bash
bash tests/run_test.sh
```

**输出示例**：
```
==========================================
📋 DocAgent 内部测试报告
==========================================
【T1】进程检查  ✅ PID: 37760
【T2】端口监听  ✅
【T3】Health    ✅ status=ok
...
📊 汇总: 9/9 全部通过 🎉
```

### 4.4 设计思路

```
集成测试设计原则：
1. 无依赖 — 不依赖测试数据，空索引也能测试降级路径
2. 全链路 — 从进程到页面，覆盖每个端点
3. 可重复 — 每次运行结果一致
4. 可读 — 输出清晰，一眼看出通过/失败
```

---

## 五、验收测试清单

### 5.1 手动验收

发布前逐项验证：

```markdown
### 功能验收
- [ ] 浏览器打开 http://127.0.0.1:8765，页面正常渲染
- [ ] 顶部显示绿色"已连接"（含文档数）
- [ ] 输入"你好"并发送，返回问候语
- [ ] 输入"什么是Transformer"，返回"未找到相关文档"（空索引时）
- [ ] 有文档索引时，输入检索内容返回正确结果
- [ ] 示例按钮（打招呼/检索/对比/合规）均可点击

### 稳定性验收
- [ ] 连续发送 10 个问题，无崩溃
- [ ] API 进程在关闭终端后依然存活
- [ ] 刷新页面后状态正常

### 安全验收
- [ ] 所有敏感信息通过环境变量注入（检查 .env 和代码）
- [ ] data/ 目录不提交到 Git（检查 .gitignore）
- [ ] 无硬编码密钥/路径
```

### 5.2 验收判断逻辑

```
LLM 输出不确定 → 验收不靠精确断言，靠行为模式：
  ✅ "意图识别正确" → intent 字段 = chat/retrieve/compare/compliance
  ✅ "降级处理正常" → 空索引时返回"未找到相关文档"
  ✅ "错误处理友好" → API 错误时返回清晰错误信息，不抛堆栈
```

---

## 六、自动化测试框架

### 6.1 当前框架

```bash
# 一键运行全部测试
bash tests/run_all.sh
```

该脚本（待创建）依次执行：
1. 单元测试（5 个文件，24 个用例）
2. 集成测试（9 项）
3. 输出汇总报告

### 6.2 理想 CI/CD 流程（TODO）

```yaml
# .github/workflows/test.yml（待创建）
name: DocAgent CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Unit tests
        run: for f in tests/test_*.py; do python3 "$f"; done
      - name: Integration tests
        run: bash tests/run_test.sh
```

### 6.3 测试覆盖率目标（TODO）

| 模块 | 当前 | 目标 |
|:----|:---:|:---:|
| 切片器 | 6 用例 | 10+ |
| 检索器 | 2 用例 | 8+ |
| Agent 核心 | 5 用例 | 10+ |
| API 模型 | 7 用例 | 10+ |
| MCP 客户端 | 4 用例 | 6+ |

---

## 七、踩坑记录

### 坑 1：LLM 首次推理慢导致测试超时

**现象**：集成测试 T5/T6 在 deepseek-r1:14b 下耗时 ~60 秒
**解决**：测试脚本的 curl timeout 设为 120 秒（`-m 120`）；开发时用 qwen2.5:3b
**教训**：集成测试的 timeout 要适配所用模型的推理速度

### 坑 2：后端测试通过 ≠ 前端可用

**现象**：API 所有端点测试通过，但浏览器中页面无法互动
**根因**：同步 XHR 被 Chrome 弃用，JS 请求被安全策略拦截
**解决**：改为异步 XHR + 正确的事件绑定
**教训**：验收测试不能只看后端，必须在浏览器中验证

### 坑 3：端口被占用、进程意外退出

**现象**：`[Errno 48] address already in use`
**根因**：旧进程未完全退出，或子进程随着父 shell 退出
**解决**：`kill -9 $(lsof -ti :8765)` 彻底清理；用 Node.js `detached: true` 启动
**教训**：测试前先确保环境干净

---

## 八、面试话术

### 当面试官问"你怎么保证 Agent 质量"

> **一句话版本**：
> "我建了三层测试体系——单元测试测逻辑、集成测试测流程、验收测试测体验。24 个单元用例覆盖切片器、检索器、API 模型；9 项集成测试覆盖所有 API 端点；还有一份手动验收清单发布前逐项检查。"

> **展开版本**：
> "Agent 项目有个特点——LLM 输出不确定，所以我们的测试策略和传统项目不一样：
> 
> 1. **确定性代码用精确断言**——切片器、API 模型这些纯逻辑代码，我写了 24 个 pytest 用例，每个都 assert 具体值
> 2. **非确定性输出用模式匹配**——Chat 接口验证 intent 字段而不是具体回答内容
> 3. **集成测试覆盖正常+异常两条路径**——问候走 chat 路径、无文档时走降级路径、空索引时走空结果路径
> 4. **验收测试手动走一遍**——打开浏览器，点几下，确保页面可交互
> 
> 这套体系在开发帮我们抓住了好几个问题：API 模型字段类型不对、降级处理遗漏、前端 JS 不兼容。现在每次改代码先跑一遍测试，所有测试通过再合并。"

---

> **Skill 版本**：v0.1 | **最后更新**：2026-06-10 | **作者**：Jason Croft
