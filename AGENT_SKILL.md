# 🧠 Agent 项目交付技能手册

> **定位**：从 0 到 1 交付一个 Agent 项目的完整方法论。
> 
> 以后任何 Agent 项目，按此手册可完成 调研→需求→设计→开发→测试→迭代→安全→发布 的全闭环。
> 
> **这不是流水账，这是决策逻辑 + 选型理由 + 踩坑记录 + 流程框架。**
>
> 版本：v0.2（Phase 1 完成）
> 最后更新：2026-06-10
> 对应项目：DocAgent（企业文档智能助手）

---

## 一、项目启动阶段

### 1.1 需求定义：做什么、不做什么

#### 切入点：从已有项目的问题出发

DocAgent 的前身是课题16（BGE-M3 Search），一个纯 RAG 搜索工具。实际使用中发现 4 个核心问题：

| 问题 | 表现 | 根因判断 |
|:----|:----|:--------|
| 检索不准 | 搜"A"返回"B"相关内容 | 切片策略太粗糙，无视文档结构 |
| 精度不够 | 前 5 条相关但不命中 | 没有二次重排机制 |
| 精确词搜不到 | "QKV"这种术语匹配失败 | 只有向量检索，缺少关键词检索 |
| 自然语言搜不好 | 口语化 Query 和笔记用语不匹配 | Query 未经改写直接编码 |

**决策**：不修课题16，从零搭建课题25。理由：
- 课题16 是手写脚本，没有框架支撑
- 我们需要 LangGraph + MCP 的 Agent 架构
- 课题16 的代码不适合公开到 GitHub

#### 需求边界

| 做 | 不做 |
|:--|:----|
| 企业文档的语义检索 | 实时数据搜索（股价、天气） |
| 多文档对比 | 非文本文件处理（图片、音视频） |
| 合规检查 | 模型训练/微调 |
| LangGraph Agent 架构 | 前端 App 开发 |

### 1.2 技术选型：为什么选这些

#### Agent 框架：LangGraph 而非 LangChain AgentExecutor

| 对比维度 | LangChain AgentExecutor | LangGraph |
|:--------|:----------------------|:----------|
| 状态管理 | 线性链 | **状态图**，支持分支/循环/并行 |
| 可观测性 | callback 有限 | **原生 state 检查** |
| 流程修改 | 需要重写 chain | **增删节点即可** |
| 复杂度 | 简单好上手 | 学习曲线稍高，上限更高 |

**结论**：文档助手有明确的流程分支（检索 vs 对比 vs 检查），状态图比线性链更合适。

#### RAG 策略对比

| 方案 | 选择 | 理由 |
|:----|:----|:----|
| 切片 | **语义边界** > 固定长度 | 固定长度会切断语义单元 |
| 向量库 | **ChromaDB** > Pinecone/Milvus | 本地运行、轻量、够用 |
| Embedding | **BGE-M3** > OpenAI/text-embedding-3 | 本地已有、免费、对标 |
| 检索 | **混合检索** > 纯向量 | 关键词精确匹配是刚需 |
| 重排 | **交叉编码器** > 无重排 | top20→top5，精度提升明显 |
| Query | **LLM 改写** > 原始 Query | 口语→术语的翻译是必要的 |

#### 模型选择

| 模型 | 用途 | 选型理由 |
|:----|:----|:--------|
| deepseek-r1:14b | LLM 推理 | 本地已有、够用 |
| BAAI/bge-m3 | Embedding | 本地已有、对标 text-embedding-3-small |

**原则**：全部用本地已有模型，零成本启动。

---

## 二、Phase 1：RAG 管道

### 2.1 架构决策

```
文档输入 → 语义切片 → 双通道索引（向量+BM25） → 混合检索 → 重排 → 结果
```

**为什么按这个顺序搭？**
1. 切片是基础——切片质量决定检索上限
2. 双通道索引是核心——向量管语义、BM25 管精确
3. 重排是兜底——用计算量换精度

### 2.2 关键设计决策

#### 切片策略

| 方案 | 优点 | 缺点 |
|:----|:----|:----|
| 固定长度 | 实现简单 | 切断语义单元 |
| **按标题层级** | 保留文档结构 | 大块标题下可能超长 |
| 按段落 | 语义完整 | 过短的段落浪费向量维度 |
| 按句子 | 粒度最细 | 上下文丢失严重 |

**最终选择**：按标题层级 + 段落边界混合。
- 先解析 Markdown 结构
- 每个块带上标题链作为上下文前缀
- 大块再按段落切分

#### 混合检索权重

向量检索权重 0.7 : BM25 权重 0.3（初始值，后续可调）。

#### BM25 索引策略（待实现）

课题16 没有 BM25。需要额外维护一个倒排索引。方案对比：
1. **rank_bm25 库**：纯 Python，内存索引，轻量但重启丢失
2. **Elasticsearch**：完整搜索引擎，但太重
3. **Meilisearch**：轻量，但不原生支持 BM25

**倾向方案**：先用 `rank_bm25` 配合 ChromaDB 的 metadata 过滤，不够再加 ES。

### 2.3 踩坑记录

#### 坑 1：课题16 的切片太粗
- **现象**：搜"注意力机制"，返回的是含"注意力"三个字的财务文档
- **根因**：500 字符硬切，不同主题的文本被混在一个 chunk 里
- **解决**：按 Markdown 标题层级切，每个 chunk 只包含一个主题
- **教训**：**切片策略直接决定了检索质量的上限**

#### 坑 2：纯向量检索无法处理精确匹配
- **现象**：搜"QKV"找不到笔记中的"QKV 矩阵计算"
- **根因**：向量检索偏语义，QKV 的向量可能靠近"Query Key Value"而不是"矩阵"
- **解决**：增加 BM25 关键词检索通道（Phase 2 实现）
- **教训**：**语义 + 关键词互补是 RAG 工程化的必修课**

### 2.4 测试策略

| 测试层级 | 覆盖内容 | 用例数 |
|:--------|:--------|:-----:|
| 单元测试 | 切片器（标题解析、层级栈、分块） | 6 |
| 单元测试 | 检索器（初始化、重排跳过） | 2 |
| 集成测试 | 待 Phase 2 补充 | - |

### 2.5 SQ（Skill 质量）自评

| 维度 | 评分 | 说明 |
|:----|:---:|:----|
| 可复用性 | ⭐⭐⭐⭐ | 切片器可复用到任何 RAG 项目 |
| 决策记录 | ⭐⭐⭐⭐ | 每个选型有对比和理由 |
| 踩坑记录 | ⭐⭐⭐ | 有但不够全，后续补充 |
| 流程完整性 | ⭐⭐⭐ | 调研→设计→开发→测试 完整 |

---

## 三、Phase 2：LangGraph Agent 核心（待填充）

> 启动时按相同结构补充：
> 3.1 架构决策
> 3.2 关键设计决策
> 3.3 踩坑记录
> 3.4 测试策略
> 3.5 SQ 自评

---

## 四、通用技能（跨 Phase）

### 4.1 Git 提交规范

| 类型 | 用途 | 示例 |
|:----|:----|:----|
| `feat` | 新功能 | `feat: 语义切片器` |
| `fix` | 修复 | `fix: 切片溢出边界条件` |
| `docs` | 文档 | `docs: AGENT_SKILL Phase1 记录` |
| `refactor` | 重构 | `refactor: 拆分检索器模块` |
| `test` | 测试 | `test: 切片器边界用例` |
| `chore` | 杂项 | `chore: 更新依赖` |

### 4.2 安全检查清单（发布前）

- [ ] 所有敏感信息通过环境变量注入
- [ ] `.gitignore` 覆盖所有不应提交的文件
- [ ] `data/` 不包含原始文档
- [ ] 无硬编码密钥/路径
- [ ] README 完整
- [ ] LICENSE 存在
- [ ] 核心函数有单元测试
- [ ] 所有测试通过
- [ ] Docker 可构建

---

> **Skill 版本**：v0.2 | **最后更新**：2026-06-10 | **作者**：Jason Croft

---

## 三、Phase 2：LangGraph Agent 核心

### 3.1 架构决策

```
用户 Query
    │
    ▼
┌──────────────────────────────────────────┐
│     LangGraph 状态图 (StateGraph)         │
│                                          │
│  意图识别节点 → RAG检索节点 → 结果整合节点 │
│                                          │
│   · 状态定义：query, intent, results,     │
│               context, answer             │
│   · Checkpoint：对话持久化                │
│   · 路由：根据 intent 分发到不同工具      │
└──────────────────────────────────────────┘
```

#### 为什么用 StateGraph 而不是简单的 Chain？

文档助手的流程有**分支**：
- 用户问"XX 条款是什么" → 直接检索
- 用户问"对比 A 和 B" → 多文档检索 + 对比
- 用户问"这个合同有没有风险" → 检索 + 合规检查

Chain 只能走直线，StateGraph 可以分叉。

### 3.2 新增文件

| 文件 | 用途 |
|:----|:----|
| `src/agent/graph.py` | StateGraph 定义、节点注册、编译 |
| `src/agent/state.py` | Agent 状态类型定义 |
| `src/agent/nodes/intent.py` | 意图识别节点（LLM 判断 Query 类型） |
| `src/agent/nodes/retrieval.py` | RAG 检索节点（复用 Phase 1 的 pipeline） |
| `src/agent/nodes/output.py` | 结果整合节点（格式化输出） |

### 3.3 迭代现有文件

| 文件 | 变更 |
|:----|:----|
| `src/rag/retriever.py` | 补全 BM25 关键词检索实现 |
| `src/rag/pipeline.py` | 可选：加一个便捷的 agent_ready_search() |

### 3.4 关键设计决策

#### 状态定义

```python
class AgentState(TypedDict):
    query: str                    # 原始用户输入
    intent: str                   # 识别后的意图（retrieve/compare/compliance）
    rewritten_queries: list       # 改写后的子查询
    retrieved_chunks: list        # 检索到的文档块
    context: str                  # 整合后的上下文
    answer: str                   # 最终回答
```

#### 意图识别策略

| Query 特征 | 意图 | 动作 |
|:----------|:----|:----|
| 含"是什么""怎么" | retrieve | 单次 RAG 检索 |
| 含"对比""区别""差异" | compare | 多文档分别检索 + 对比 |
| 含"合规""风险""检查" | compliance | 检索 + 规则匹配 |

使用 LLM 进行意图分类，而不是规则匹配——更灵活，能处理边界情况。

### 3.5 踩坑记录（待补充）

### 3.6 测试策略

| 层级 | 内容 | 用例数 |
|:----|:----|:-----:|
| 单元测试 | 状态图初始化、节点注册 | 2 |
| 单元测试 | 意图识别 prompt 模板 | 3 |
| 集成测试 | Query→检索→输出的完整链路 | 2 |

---

## 四、Phase 3：MCP 协议集成

### 4.1 架构决策

```
┌──────────────────────────────────────────────┐
│              LangGraph Agent                  │
│                                              │
│  意图识别 → RAG检索 → 结果整合                │
│       │            │           │              │
│       ▼            ▼           ▼              │
│   ┌────────────────────────────────────┐      │
│   │        MCP 客户端层                │      │
│   │  (Agent 通过 MCP 调用外部工具)      │      │
│   └──────────┬─────────────────────────┘      │
│              │                                 │
└──────────────┼─────────────────────────────────┘
               │ MCP 协议
┌──────────────▼─────────────────────────────────┐
│              MCP Server 层                      │
│                                                │
│  ┌──────────────┐  ┌──────────┐  ┌──────────┐ │
│  │ 文档检索     │  │ 文档对比  │  │ 合规检查  │ │
│  │ MCP Server  │  │ MCP Svr  │  │ MCP Svr  │ │
│  └──────────────┘  └──────────┘  └──────────┘ │
└────────────────────────────────────────────────┘
```

#### 为什么引入 MCP？

| 问题 | 没有 MCP | 有 MCP |
|:----|:--------|:------|
| 工具调用方式 | Agent 直接调 Python 函数 | 通过标准化协议调用 |
| 工具扩展 | 改代码、重部署 | 加一个 MCP Server 就行 |
| 跨语言工具 | Python only | 任何语言（JS/Go/Rust） |
| 工具发现 | 硬编码 | MCP 的 ListTools 动态发现 |

MCP 的核心价值不在于"能做什么"，而在于**标准化**——工具和 Agent 解耦，任意一方变化不影响另一方。

#### MCP 三要素

```
Host（主机）：LangGraph Agent进程，发起连接
    │
Client（客户端）：每个连接一个客户端实例，管理通信
    │
Server（服务器）：提供具体工具（Stdio/SSE）
```

### 4.2 新增文件

| 文件 | 用途 |
|:----|:----|
| `src/mcp/server.py` | MCP Server 定义（文档检索、文档对比、合规检查） |
| `src/mcp/client.py` | MCP 客户端工具包装（Agent 通过它调用工具） |
| `tests/test_mcp.py` | MCP 测试 |

### 4.3 迭代现有文件

| 文件 | 变更 |
|:----|:----|
| `src/agent/graph.py` | 检索节点改为通过 MCP 调用工具 |
| `src/agent/nodes/retrieval.py` | 支持 MCP 工具调用路径 |
| `src/agent/nodes/output.py` | 支持 MCP 的对比/合规结果处理 |

### 4.4 关键设计决策

#### MCP Server 设计

| Server | 工具 | 用途 |
|:-------|:----|:----|
| `doc_retrieval_server` | `search_documents` | 文档检索（接收 Query，返回检索结果） |
| `doc_comparison_server` | `compare_documents` | 多文档对比（接收文档 IDs，返回对比结果） |
| `compliance_server` | `check_compliance` | 合规检查（接收条款，返回风险项） |

#### 通信方式选择

| 方式 | 优点 | 缺点 | 选择 |
|:----|:----|:----|:----|
| **Stdio** | 简单、无网络开销 | 进程绑定 | Phase 3 用此 |
| SSE | 可远程、独立进程 | 需要 HTTP 服务 | Phase 4 升级为此 |

Phase 3 先用 Stdio 通信（Agent 和 MCP Server 在同一进程），Phase 4 服务化时升级为 SSE。

### 4.5 踩坑记录（待补充）

### 4.6 测试策略

| 层级 | 内容 | 用例数 |
|:----|:----|:-----:|
| 单元测试 | MCP Server 工具注册、工具调用 | 3 |
| 集成测试 | Agent 通过 MCP 完成检索 → 回答 | 2 |

---

## 五、Phase 4：FastAPI 服务化 + Docker

### 5.1 架构决策

```
┌─────────────────────────────────────────────────────┐
│                  用户层                              │
│                                                     │
│  ┌──────────────┐   ┌──────────────────────────┐    │
│  │ Streamlit UI  │   │   REST API 客户端         │    │
│  │（网页交互）   │   │   curl / Postman / 其他App │    │
│  └──────┬───────┘   └──────────┬───────────────┘    │
│         │                      │                    │
│         ▼                      ▼                    │
│  ┌──────────────────────────────────────────────┐   │
│  │           FastAPI 服务层                      │   │
│  │                                              │   │
│  │  POST /api/chat      →  LangGraph Agent      │   │
│  │  POST /api/search    →  RAG 检索（直连）      │   │
│  │  GET  /api/health    → 健康检查               │   │
│  │  GET  /api/stats     → 索引统计               │   │
│  └──────────────────┬───────────────────────────┘   │
│                     │                               │
│                     ▼                               │
│  ┌──────────────────────────────────────────────┐   │
│  │           LangGraph Agent + MCP              │   │
│  │           （内部调用，不变）                   │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

#### 为什么要 REST API 包装

Agent 目前只能通过 Python 代码调用。服务化之后：
- 任何语言都可以调用（curl/JS/Java/Go）
- 可以嵌入到其他产品
- 可以容器化部署

#### API 端点设计原则

| 原则 | 说明 |
|:----|:-----|
| 无状态 | 每次请求独立，通过 thread_id 管理对话 |
| 异步 | 所有端点支持 async |
| 标准化 | 输入输出用 Pydantic 模型 |
| 健康检查 | /api/health 用于 Docker 编排 |

### 5.2 新增文件

| 文件 | 用途 |
|:----|:----|
| `src/api/main.py` | FastAPI 应用定义、路由注册 |
| `src/api/models.py` | 请求/响应 Pydantic 模型 |
| `Dockerfile` | 多阶段构建 Docker 镜像 |
| `docker-compose.yml` | 编排 Agent + LangFuse |
| `src/api/startup.py` | 启动时加载模型和 Agent |

### 5.3 迭代现有文件

| 文件 | 变更 |
|:----|:----|
| `src/config/settings.py` | 新增 API 相关配置 |
| `requirements.txt` | 新增 streamlit 依赖 |

### 5.4 关键设计决策

#### 为什么用 Streamlit 作 UI

| 方案 | 优点 | 缺点 | 选择 |
|:----|:----|:----|:----|
| Streamlit | 纯 Python、最快速搭建 | 不够灵活、定制有限 | Phase 4 |
| React/Vue | 灵活、好看 | 需要前端知识 | 后续 |
| Gradio | ML 生态好 | 不适合企业应用 | ❌ |

先用 Streamlit 快速出界面，后续可以替换为任何前端。

#### Docker 构建策略

多阶段构建：
1. build 阶段：安装依赖 + 复制代码
2. run 阶段：轻量运行镜像

#### docker-compose 服务

| 服务 | 用途 | 端口 |
|:----|:----|:----|
| doc-agent | Agent API | 8765 |
| langfuse | 可观测性 | 3000 |

### 5.5 踩坑记录（待补充）

### 5.6 测试策略

| 层级 | 内容 | 用例数 |
|:----|:----|:-----:|
| 单元测试 | API 模型、健康检查 | 3 |
| 集成测试 | /api/chat 端点调用 | 2 |
| 容器测试 | Docker 构建、启动 | 1（手动） |

---

## 七、测试体系（通用技能·跨项目）

### 7.1 测试方法论

三个层级：

| 层级 | 覆盖范围 | 自动化 | 触发时机 |
|:----|:--------|:-----:|:--------|
| **单元测试** | 单个函数/类（切片器、检索器、API模型） | ✅ pytest | 每次代码修改 |
| **集成测试** | API 端点全链路（Health/Chat/Search/页面） | ✅ `bash tests/run_test.sh` | 每次启动前 |
| **验收测试** | 浏览器交互验证 | ❌ 手动 | 发布前 |

### 7.2 单元测试清单

| 文件 | 覆盖内容 | 用例数 | 命令 |
|:----|:--------|:-----:|:----|
| `tests/test_splitter.py` | 语义切片器 | 6 | `python3 tests/test_splitter.py` |
| `tests/test_retriever.py` | 混合检索器 | 2 | `python3 tests/test_retriever.py` |
| `tests/test_agent.py` | Agent 核心 | 5 | `python3 tests/test_agent.py` |
| `tests/test_mcp.py` | MCP 客户端 | 4 | `python3 tests/test_mcp.py` |
| `tests/test_api.py` | API 数据模型 | 7 | `python3 tests/test_api.py` |

### 7.3 集成测试清单

**脚本**：`bash tests/run_test.sh`

| 编号 | 测试项 | 验证内容 |
|:---:|:------|:--------|
| T1 | 进程检查 | PID 存在 |
| T2 | 端口监听 | LISTEN 状态 |
| T3 | GET /api/health | status=ok |
| T4 | GET /api/stats | 返回统计 |
| T5 | POST /api/chat（问候） | intent=chat |
| T6 | POST /api/chat（检索-无文档） | intent=retrieve，降级处理 |
| T7 | POST /api/search（空索引） | results=[] |
| T8 | GET /（HTML页面） | 含 DocAgent 标题 |
| T9 | Ollama 模型检查 | 三个模型可用 |

### 7.4 验收测试（手动）

- [ ] 浏览器打开 http://127.0.0.1:8765，页面渲染
- [ ] 顶部状态显示绿色"已连接"
- [ ] 输入"你好"并发送，返回问候
- [ ] 示例按钮可用

### 7.5 已知问题

| 问题 | 原因 | 缓解 |
|:----|:----|:-----|
| deepseek-r1:14b 首次推理耗时 ~60s | 模型加载 | 预热：先发一次简单请求 |
| Python 3.9 不支持 `list[X] \| None` | 语法限制 | 使用 `= None` |
| 进程随 shell 退出终止 | 后台管理 | 使用 `nohup + disown` 或 Node.js detached spawn |
| `uvicorn` 不在 PATH | pip 安装位置 | 使用 `python3 -m uvicorn` |

### 7.6 测试经验

> 1. 后端通过 ≠ 前端可用—浏览器 JS 可能被安全策略拦截
> 2. deepseek 首次推理需 ~60s，测试 timeout 要设够
> 3. 端口被占先 `kill -9`，用 `lsof -i :PORT` 确认
> 4. 集成测试要覆盖正常+异常两条路径（问候+检索+空索引）

---

## 六、测试体系（通用技能·跨项目）

### 6.1 测试方法论

DocAgent 项目的测试分为三个层级：

| 层级 | 覆盖范围 | 自动化 | 触发时机 |
|:----|:--------|:-----:|:--------|
| **单元测试** | 单个函数/类（切片器、检索器、API模型） | ✅ pytest | 每次代码修改 |
| **集成测试** | API 端点全链路（Health/Chat/Search/页面） | ✅ ==========================================
📋 DocAgent 内部测试报告
生成时间: 2026-06-10 12:39:01
==========================================

【T1】进程检查
  ✅ API 进程运行中 (PID: 37760
37887
37885)

【T2】端口 8765 监听检查
  ✅ 端口 8765 正在监听

【T3】GET /api/health
  ✅ 响应正常，模型已加载

【T4】GET /api/stats
  ✅ 响应正常

【T5】POST /api/chat（问候意图）
  ✅ 意图识别: chat

【T6】POST /api/chat（检索意图，无文档时）
  ❌ 响应异常

【T7】POST /api/search（空索引时）
  ❌ 响应异常: 

【T8】GET /（HTML UI 页面）
  ❌ 页面异常（0 bytes）

【T9】Ollama 模型检查
  ✅ 模型: qwen2.5:3b, bge-m3:latest, deepseek-r1:14b

==========================================
📊 汇总: 6 通过 / 9 总计
❌ 3 项失败
========================================== | 每次启动前 |
| **验收测试** | 浏览器交互验证 | ❌ 手动 | 发布前 |

**核心原则**：
- 单元测试保证代码逻辑正确
- 集成测试保证服务能跑通
- 验收测试保证用户能用

### 6.2 单元测试清单

| 测试文件 | 覆盖内容 | 用例数 | 命令 |
|:--------|:--------|:-----:|:----|
|  | 语义切片器（标题解析、层级栈、分块） | 6 | ✅ 所有测试通过！ |
|  | 混合检索器（初始化、重排跳过） | 2 | ✅ 所有测试通过！ |
|  | Agent 核心（意图识别、状态默认值） | 5 | ✅ 所有测试通过！ |
|  | MCP 客户端（工具包装、参数定义） | 4 | ✅ 所有测试通过！ |
|  | API 数据模型（请求/响应验证） | 7 | ✅ 所有 API 测试通过！ |

### 6.3 集成测试清单

**脚本位置**：==========================================
📋 DocAgent 内部测试报告
生成时间: 2026-06-10 12:40:30
==========================================

【T1】进程检查
  ✅ API 进程运行中 (PID: 41313
37887
37885)

【T2】端口 8765 监听检查
  ✅ 端口 8765 正在监听

【T3】GET /api/health
  ✅ 响应正常，模型已加载

【T4】GET /api/stats
  ✅ 响应正常

【T5】POST /api/chat（问候意图）
  ❌ 响应异常

【T6】POST /api/chat（检索意图，无文档时）
  ❌ 响应异常

【T7】POST /api/search（空索引时）
  ❌ 响应异常: 

【T8】GET /（HTML UI 页面）
  ❌ 页面异常（0 bytes）

【T9】Ollama 模型检查
  ✅ 模型: qwen2.5:3b, bge-m3:latest, deepseek-r1:14b

==========================================
📊 汇总: 5 通过 / 9 总计
❌ 4 项失败
==========================================

**测试项（9项）**：

| 编号 | 测试项 | 验证内容 | 预期 |
|:---:|:------|:--------|:----|
| T1 | 进程检查 | API 进程是否在运行 | PID 存在 |
| T2 | 端口检查 | 8765 端口是否监听 | LISTEN 状态 |
| T3 | GET /api/health | 服务健康状态 | status=ok |
| T4 | GET /api/stats | 索引统计 | 返回统计信息 |
| T5 | POST /api/chat（问候） | 意图识别 | intent=chat |
| T6 | POST /api/chat（检索） | 无文档时降级处理 | intent=retrieve |
| T7 | POST /api/search | 空索引时返回空结果 | results=[] |
| T8 | GET /（页面） | HTML UI 正常返回 | 包含 DocAgent 标题 |
| T9 | Ollama 模型检查 | 依赖模型可用 | 三个模型都在 |

**运行命令**：
==========================================
📋 DocAgent 内部测试报告
生成时间: 2026-06-10 12:40:46
==========================================

【T1】进程检查
  ✅ API 进程运行中 (PID: 37887
37885)

【T2】端口 8765 监听检查
  ❌ 端口未监听

【T3】GET /api/health
  ❌ 响应异常: 

【T4】GET /api/stats
  ❌ 响应异常: 

【T5】POST /api/chat（问候意图）
  ❌ 响应异常

【T6】POST /api/chat（检索意图，无文档时）
  ❌ 响应异常

【T7】POST /api/search（空索引时）
  ❌ 响应异常: 

【T8】GET /（HTML UI 页面）
  ❌ 页面异常（0 bytes）

【T9】Ollama 模型检查
  ✅ 模型: qwen2.5:3b, bge-m3:latest, deepseek-r1:14b

==========================================
📊 汇总: 2 通过 / 9 总计
❌ 7 项失败
==========================================

### 6.4 验收测试清单（手动）

发布前手动验证：

- [ ] 打开浏览器访问 http://127.0.0.1:8765，页面正常渲染
- [ ] 顶部状态栏显示绿色"已连接"
- [ ] 输入"你好"并发送，Agent 返回问候语
- [ ] 输入"什么是Transformer"，Agent 回"未找到相关文档"
- [ ] 示例按钮（打招呼/检索/对比/合规）均可点击
- [ ] API 进程在关闭终端后依然存活

### 6.5 已知问题与边界条件

| 问题 | 原因 | 缓解措施 |
|:----|:----|:--------|
| deepseek-r1:14b 首次推理慢(~60s) | 模型加载到内存 | 预热：先发一次简单请求 |
| Chrome 同步 XHR 被弃用 | 安全策略 | 使用异步 XHR |
| Python 3.9 不支持  | 语法限制 | 使用  |
|  CLI 可能不在 PATH | pip 安装位置 | 使用  |
| 进程随 shell 退出而终止 | 后台进程管理 | 使用  +  |

### 6.6 测试经验总结

> **关键经验**
> 1. 单元测试先写后改——改代码前先跑一遍，确保改后不破坏已有功能
> 2. 集成测试要覆盖正常和异常两条路径——问候走 chat 路径，检索走 retrieve 路径
> 3. 后端测试通过 ≠ 前端可用——浏览器中的 JS 请求可能被安全策略拦截
> 4. deepseek-r1:14b 的首次推理耗时 ~60 秒，测试脚本的 timeout 要设够
> 5. 端口被占用时要先  再重启，用  确认

---

## 五、Phase 5：可观测性 + 评测体系

### 5.1 架构决策

```
┌──────────────────────────────────────────────────┐
│                  LangFuse                        │
│                                                  │
│  ┌──────────────┐  ┌──────────┐  ┌────────────┐ │
│  │   Trace      │  │  Prompt  │  │ Evaluation │ │
│  │  可视化       │  │  管理    │  │  评测      │ │
│  └──────────────┘  └──────────┘  └────────────┘ │
│                                                  │
│  每个 Agent 调用自动打 span：                      │
│  · LLM 调用（模型、耗时、token 数）                │
│  · 工具调用（MCP Server 请求/响应）                │
│  · 节点流转（intent → retrieval → output）        │
└──────────────────────────────────────────────────┘
```

#### 为什么需要可观测性？

| 没有 Trace | 有 LangFuse Trace |
|:----------|:-----------------|
| Agent 出问题不知道在哪一步 | 每一步的输入/输出/耗时一目了然 |
| 不知道 LLM 调用了多少次 | 每次 LLM 调用的 token 数可统计 |
| 不知道哪个 Prompt 效果好 | 不同 Prompt 版本的对比数据 |
| 线上问题靠猜 | 直接看 trace 图定位 |

#### 评测体系架构

```
测试用例集（20 条）
│
├── 正常场景（10 条）：问候、检索、对比、合规
├── 边界场景（5 条）：空查询、超长输入、特殊字符
└── 异常场景（5 条）：空索引、未知意图、工具失败
        │
        ▼
LLM-as-Judge 自动评分
│  评估维度：准确性、完整性、安全性
│
├── 通过率 ≥ 80% → 门禁通过
└── 通过率 < 80% → 阻止发布
```

### 5.2 新增文件

| 文件 | 用途 |
|:----|:----|
| `src/telemetry/tracer.py` | LangFuse Trace 初始化 + 装饰器 |
| `tests/evaluation/test_suite.json` | 20 条测试用例 |
| `tests/evaluation/run_eval.py` | LLM-as-Judge 评测脚本 |
| `.env.example` | 新增 LangFuse 密钥占位 |

### 5.3 迭代文件

| 文件 | 变更 |
|:----|:----|
| `src/agent/graph.py` | Graph 中接入 LangFuse trace |
| `src/rag/pipeline.py` | 检索过程打 span |
| `tests/run_test.sh` | 新增评测步骤 |

### 5.4 关键设计

#### Trace 数据结构

```
Agent Run (thread_id)
  ├── Node: intent
  │   ├── LLM Call: intent detection → token: 50
  │   └── Output: intent=retrieve
  ├── Node: retrieval
  │   ├── Tool Call: search_documents → MCP → ChromaDB
  │   ├── LLM Call: query rewrite → token: 80
  │   └── Output: 5 chunks found
  └── Node: output
      └── LLM Call: generate answer → token: 350
```

#### 评测评分标准

| 维度 | 权重 | 评分方式 |
|:----|:----:|:--------|
| 准确性 | 40% | 是否基于检索结果回答 |
| 完整性 | 30% | 是否回答了用户问题的所有方面 |
| 安全性 | 30% | 是否有拒绝回答/脱敏/不输出有害内容 |
